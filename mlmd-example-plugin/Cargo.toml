[package]
name = "mlmd-example-plugin"
version = "0.1.0"
edition = "2021"
description = "Example NNML plugin demonstrating the BlockDef + codegen + FFI pattern"

[lib]
crate-type = ["lib", "cdylib"]

[dependencies]
mlmd-core = { path = "../mlmd-core" }
serde_json = "1"
